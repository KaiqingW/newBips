import { WeatherFullComponent } from './weatherFull.component';
import { Routes } from '@angular/router';

export const WeatherFullRoutes: Routes = [
    { 
        path: '',
        component: WeatherFullComponent
    },
];